const navBarEvents = () => {
  const navLinks = Array.from(document.getElementsByClassName('navLink'));
  navLinks.forEach((navLink) => {
navLink.addEventListener('click', () => {
  if (else.target.id === '') {
firebase.auth();

  }
});
  });
console.error('testing nav bar');
};

export default { navBarEvents, };